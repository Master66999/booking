import { render, fireEvent, screen } from "@testing-library/react";
import App from "./App";

// This test is removed because the App does not have elements with data-testid 'currentNumber' and 'addOne'.
